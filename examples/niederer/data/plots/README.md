# Plot Fixtures

This directory contains small Plotly JSON fixtures used by the shipped
Niederer example workspace.

- `niederer_signal.json`: a simple two-trace activation-style line chart for
  the `4d-graph` shortcode
