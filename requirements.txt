panel>=1.3.0
pyyaml>=6.0
tornado>=6.3
# vtk stack pinned: trame-vtk >= 2.11 stopped exposing window.renderWindow in the
# OfflineLocalView export, which breaks the figure's live scalar swap (time
# animation, field switch, camera sync). These versions are verified to expose it.
pyvista[jupyter]==0.47.3
vtk==9.6.1
trame==3.12.0
trame-vtk==2.10.2
trame-vuetify==3.2.1
quarto>=0.1.0
weasyprint>=60.0
plotly>=5.0.0
kaleido==0.2.1
cryptography>=41.0.0
