# v0.1.1 Manual Release Sweep

Candidate: `4dpapers-0.1.1-rc-final`
Expected version: `0.1.1`
Automated status on 2026-07-15: `403 passed, 11 skipped`; full and Quick smoke pass.

Allow about 15 minutes. Mark every item pass or fail. Stop the release if a
figure is blank, an export is missing, the source data changes, the browser
shows an uncaught error, or the documented launch command does not work.

## 1. Full paper mode

- [ ] Start the candidate with the README command, replacing the published
  image with `4dpapers-0.1.1-rc-final`.
- [ ] Open the dashboard in a private browser window and confirm the file tree,
  editor, settings, and Docs entry load.
- [ ] Make a harmless text edit and compile the Niederer example.
- [ ] Rotate a figure, change a field, and play the time series.
- [ ] Download HTML and open it in a new tab; the interactive figure still works.
- [ ] Export PDF and open it; text and figure fallbacks are visible and legible.
- [ ] Check the browser console: no uncaught application errors.

## 2. Quick Export beta

Use the simulation case you intend to show at launch:

```bash
IMAGE=4dpapers-0.1.1-rc-final \
  ./development/quick-export/4d-quick.sh /absolute/path/to/case.foam \
  --output-dir /tmp/4dpapers-manual-quick
```

- [ ] The preview loads with a live figure; rotate it, change a field, and play
  the animation when the case has timesteps.
- [ ] Enter a title, author, and short description, then export.
- [ ] The downloaded standalone HTML opens from disk with the live figure.
- [ ] `/tmp/4dpapers-manual-quick` contains only the figure HTML and standalone
  HTML—no QMD, PNG, cache, state, or copied source data.
- [ ] The original case directory has no new or modified files.
- [ ] Stop the launcher with `Ctrl-C`; `docker ps -a` shows no Quick container.

## 3. GitHub release surface

- [ ] Open the README while logged out and verify its image, repository links,
  support links, and root live-demo link.
- [ ] Confirm the root live demo creates interactive canvases and has no uncaught
  browser errors. Do not require the deferred `/demo/` or `heart-demo.html` paths.
- [ ] Confirm the copy calls the project **source-available**, identifies Quick
  Export as **beta**, and does not overclaim experimental format support.
- [ ] Confirm the GitHub Release notes use the same version, image tag, claims,
  and rollback reference as the README.

## 4. Release gate after this sweep

Only proceed if every item above passes:

1. Review and commit the candidate; record the commit SHA.
2. Push a PR and wait for regression, image-smoke, and existing example-preview checks.
3. Merge and tag `v0.1.1`; do not move or overwrite `v0.1.0`.
4. Wait for both `amd64` and `arm64` images, then test the README command from a
   fresh clone using the published digest.
5. Verify the existing root live demo remains healthy; do not deploy the deferred
   marketing website from `website-v0.1.2`.
6. Publish the GitHub Release, then launch from the founder's personal LinkedIn profile.
