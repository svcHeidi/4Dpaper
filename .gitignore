# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/

# Quarto build outputs
_output/
_freeze/
*_files/

# Runtime state (created at serve time)
state/

# Local machine-specific config (contains absolute paths)
dashboard/config.yaml
_shortcuts.yml

# Secrets — never commit these
.env
*.pem
*.key
*.crt

# macOS
.DS_Store

/.luarc.json

/.quarto/
**/*.quarto_ipynb

# Local brainstorming workspace
.superpowers/

# Runtime cache
.cache/

# Default local paper workspace used by docker compose
workspace/

# Generated outputs (large, not for version control)
*.standalone-*.html
example_heart-standalone*.html
# Root-level PDFs (compiled exports belong in _output/, not the repo root)
/*.pdf

# Local demo/presentation artifacts
/Presentation_*.pptx
/*.pptx
/Screen Recording *.mov
~$*.pptx
/build_presentation.py

# Pre-refactor backups and one-off scripts (scratch work)
original_4dpaper.py
build_4dpaper.py
create_new_entrypoint.py
extract_mesh.py
extract_module.py
extract_parser.py
master_refactor.py
split_4dpaper_refactor.py
move_golden_js.py
new_header.py
tmp_*.py

# Internal agent/AI notes (not user-facing)
AGENT_FIGURES_CAPABILITIES.md
